# Contributing

Create focused changes, retain type hints and docstrings, and never modify frozen files under `models/` without explicit compatibility approval. Run `make format lint test` before a pull request. Include tests for behavior changes and document security-sensitive trade-offs.
